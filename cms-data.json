[
  {
    "slug": "wordpress",
    "name": "WordPress",
    "category": "Open-source CMS",
    "silktide_support": "built_in",
    "silktide_help_url": "https://help.silktide.com/en/articles/8117667-how-to-integrate-silktide-with-wordpress",
    "edit_link_strategy": "built_in",
    "retest_strategy": "built_in",
    "notes": "**Deep-link support:** Built-in (Silktide handles the Edit button).\n**What to do:** Follow the Silktide integration guide in `silktide_help_url`.\n**Editor experience:** Users click **Edit** in Silktide and are taken to the correct item in the CMS.",
    "notes_original": "Silktide provides a built-in integration for deep-linking (Edit button).",
    "editor_context": {
      "strategy": "built_in",
      "silktide_support": "built_in",
      "primary_docs": "https://help.silktide.com/en/articles/8117667-how-to-integrate-silktide-with-wordpress"
    },
    "template_md": "# WordPress → Silktide deep-link integration template\n\n**Silktide support:** Built-in (Silktide)  \n**Recommended deep-link approach:** Built-in integration\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **WordPress**.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Silktide’s built-in integration\nFollow the Silktide Help Center guide for **WordPress**: https://help.silktide.com/en/articles/8117667-how-to-integrate-silktide-with-wordpress\nOnce configured, Silktide should show an **Edit** button that opens the correct editor screen.\n\n### Fallback (if the built-in integration can’t determine your editor URL)\nIf your setup is non-standard (custom routing, headless frontends, etc.), use the **Meta tag format** below to provide Silktide with an explicit `editorUrl` per page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "drupal",
    "name": "Drupal (7–11)",
    "category": "Open-source CMS",
    "silktide_support": "built_in",
    "silktide_help_url": "https://help.silktide.com/en/articles/8020843-how-to-integrate-silktide-with-drupal-8",
    "edit_link_strategy": "built_in",
    "retest_strategy": "built_in",
    "notes": "**Deep-link support:** Built-in (Silktide handles the Edit button).\n**What to do:** Follow the Silktide integration guide in `silktide_help_url`.\n**Editor experience:** Users click **Edit** in Silktide and are taken to the correct item in the CMS.",
    "notes_original": "Silktide supports Drupal 7–11 via built-in integration guides.",
    "editor_context": {
      "strategy": "built_in",
      "silktide_support": "built_in",
      "primary_docs": "https://help.silktide.com/en/articles/8020843-how-to-integrate-silktide-with-drupal-8"
    },
    "template_md": "# Drupal (7–11) → Silktide deep-link integration template\n\n**Silktide support:** Built-in (Silktide)  \n**Recommended deep-link approach:** Built-in integration\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Drupal (7–11)**.\n\n## CMS-specific considerations\nSilktide supports Drupal 7–11 via built-in integration guides.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Silktide’s built-in integration\nFollow the Silktide Help Center guide for **Drupal (7–11)**: https://help.silktide.com/en/articles/8020843-how-to-integrate-silktide-with-drupal-8\nOnce configured, Silktide should show an **Edit** button that opens the correct editor screen.\n\n### Fallback (if the built-in integration can’t determine your editor URL)\nIf your setup is non-standard (custom routing, headless frontends, etc.), use the **Meta tag format** below to provide Silktide with an explicit `editorUrl` per page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "aem",
    "name": "Adobe Experience Manager (AEM)",
    "category": "Enterprise DXP",
    "silktide_support": "built_in",
    "silktide_help_url": "https://help.silktide.com/en/articles/8020840-how-to-integrate-silktide-with-adobe-experience-manager-aem",
    "edit_link_strategy": "built_in",
    "retest_strategy": "built_in",
    "notes": "**Deep-link support:** Built-in (Silktide handles the Edit button).\n**What to do:** Follow the Silktide integration guide in `silktide_help_url`.\n**Editor experience:** Users click **Edit** in Silktide and are taken to the correct item in the CMS.",
    "notes_original": "Built-in AEM integration.",
    "editor_context": {
      "strategy": "built_in",
      "silktide_support": "built_in",
      "primary_docs": "https://help.silktide.com/en/articles/8020840-how-to-integrate-silktide-with-adobe-experience-manager-aem"
    },
    "template_md": "# Adobe Experience Manager (AEM) → Silktide deep-link integration template\n\n**Silktide support:** Built-in (Silktide)  \n**Recommended deep-link approach:** Built-in integration\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Adobe Experience Manager (AEM)**.\n\n## CMS-specific considerations\nBuilt-in AEM integration.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Silktide’s built-in integration\nFollow the Silktide Help Center guide for **Adobe Experience Manager (AEM)**: https://help.silktide.com/en/articles/8020840-how-to-integrate-silktide-with-adobe-experience-manager-aem\nOnce configured, Silktide should show an **Edit** button that opens the correct editor screen.\n\n### Fallback (if the built-in integration can’t determine your editor URL)\nIf your setup is non-standard (custom routing, headless frontends, etc.), use the **Meta tag format** below to provide Silktide with an explicit `editorUrl` per page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "umbraco",
    "name": "Umbraco",
    "category": "Open-source/.NET CMS",
    "silktide_support": "built_in",
    "silktide_help_url": "https://help.silktide.com/en/articles/8020841-how-to-integrate-silktide-with-umbraco",
    "edit_link_strategy": "built_in",
    "retest_strategy": "built_in",
    "notes": "**Deep-link support:** Built-in (Silktide handles the Edit button).\n**What to do:** Follow the Silktide integration guide in `silktide_help_url`.\n**Editor experience:** Users click **Edit** in Silktide and are taken to the correct item in the CMS.",
    "notes_original": "Built-in Umbraco integration.",
    "editor_context": {
      "strategy": "built_in",
      "silktide_support": "built_in",
      "primary_docs": "https://help.silktide.com/en/articles/8020841-how-to-integrate-silktide-with-umbraco"
    },
    "template_md": "# Umbraco → Silktide deep-link integration template\n\n**Silktide support:** Built-in (Silktide)  \n**Recommended deep-link approach:** Built-in integration\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Umbraco**.\n\n## CMS-specific considerations\nBuilt-in Umbraco integration.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Silktide’s built-in integration\nFollow the Silktide Help Center guide for **Umbraco**: https://help.silktide.com/en/articles/8020841-how-to-integrate-silktide-with-umbraco\nOnce configured, Silktide should show an **Edit** button that opens the correct editor screen.\n\n### Fallback (if the built-in integration can’t determine your editor URL)\nIf your setup is non-standard (custom routing, headless frontends, etc.), use the **Meta tag format** below to provide Silktide with an explicit `editorUrl` per page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "sitefinity",
    "name": "Progress Sitefinity",
    "category": "Enterprise CMS",
    "silktide_support": "built_in",
    "silktide_help_url": "https://help.silktide.com/en/articles/8020844-how-to-integrate-silktide-with-sitefinity",
    "edit_link_strategy": "built_in",
    "retest_strategy": "built_in",
    "notes": "**Deep-link support:** Built-in (Silktide handles the Edit button).\n**What to do:** Follow the Silktide integration guide in `silktide_help_url`.\n**Editor experience:** Users click **Edit** in Silktide and are taken to the correct item in the CMS.",
    "notes_original": "Built-in Sitefinity integration.",
    "editor_context": {
      "strategy": "built_in",
      "silktide_support": "built_in",
      "primary_docs": "https://help.silktide.com/en/articles/8020844-how-to-integrate-silktide-with-sitefinity"
    },
    "template_md": "# Progress Sitefinity → Silktide deep-link integration template\n\n**Silktide support:** Built-in (Silktide)  \n**Recommended deep-link approach:** Built-in integration\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Progress Sitefinity**.\n\n## CMS-specific considerations\nBuilt-in Sitefinity integration.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Silktide’s built-in integration\nFollow the Silktide Help Center guide for **Progress Sitefinity**: https://help.silktide.com/en/articles/8020844-how-to-integrate-silktide-with-sitefinity\nOnce configured, Silktide should show an **Edit** button that opens the correct editor screen.\n\n### Fallback (if the built-in integration can’t determine your editor URL)\nIf your setup is non-standard (custom routing, headless frontends, etc.), use the **Meta tag format** below to provide Silktide with an explicit `editorUrl` per page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "craftcms",
    "name": "Craft CMS",
    "category": "CMS",
    "silktide_support": "built_in",
    "silktide_help_url": "https://help.silktide.com/en/articles/8020842-how-to-integrate-silktide-with-craft-cms-3",
    "edit_link_strategy": "built_in",
    "retest_strategy": "built_in",
    "notes": "**Deep-link support:** Built-in (Silktide handles the Edit button).\n**What to do:** Follow the Silktide integration guide in `silktide_help_url`.\n**Editor experience:** Users click **Edit** in Silktide and are taken to the correct item in the CMS.",
    "notes_original": "Built-in Craft CMS integration (guide references Craft CMS 3).",
    "editor_context": {
      "strategy": "built_in",
      "silktide_support": "built_in",
      "primary_docs": "https://help.silktide.com/en/articles/8020842-how-to-integrate-silktide-with-craft-cms-3"
    },
    "template_md": "# Craft CMS → Silktide deep-link integration template\n\n**Silktide support:** Built-in (Silktide)  \n**Recommended deep-link approach:** Built-in integration\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Craft CMS**.\n\n## CMS-specific considerations\nBuilt-in Craft CMS integration (guide references Craft CMS 3).\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Silktide’s built-in integration\nFollow the Silktide Help Center guide for **Craft CMS**: https://help.silktide.com/en/articles/8020842-how-to-integrate-silktide-with-craft-cms-3\nOnce configured, Silktide should show an **Edit** button that opens the correct editor screen.\n\n### Fallback (if the built-in integration can’t determine your editor URL)\nIf your setup is non-standard (custom routing, headless frontends, etc.), use the **Meta tag format** below to provide Silktide with an explicit `editorUrl` per page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "terminalfour",
    "name": "Terminalfour",
    "category": "Enterprise / Higher-Ed CMS",
    "silktide_support": "built_in",
    "silktide_help_url": "https://help.silktide.com/en/articles/8170116-how-to-integrate-silktide-with-terminalfour",
    "edit_link_strategy": "built_in",
    "retest_strategy": "built_in",
    "notes": "**Deep-link support:** Built-in (Silktide handles the Edit button).\n**What to do:** Follow the Silktide integration guide in `silktide_help_url`.\n**Editor experience:** Users click **Edit** in Silktide and are taken to the correct item in the CMS.",
    "notes_original": "Built-in Terminalfour integration.",
    "editor_context": {
      "strategy": "built_in",
      "silktide_support": "built_in",
      "primary_docs": "https://help.silktide.com/en/articles/8170116-how-to-integrate-silktide-with-terminalfour"
    },
    "template_md": "# Terminalfour → Silktide deep-link integration template\n\n**Silktide support:** Built-in (Silktide)  \n**Recommended deep-link approach:** Built-in integration\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Terminalfour**.\n\n## CMS-specific considerations\nBuilt-in Terminalfour integration.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Silktide’s built-in integration\nFollow the Silktide Help Center guide for **Terminalfour**: https://help.silktide.com/en/articles/8170116-how-to-integrate-silktide-with-terminalfour\nOnce configured, Silktide should show an **Edit** button that opens the correct editor screen.\n\n### Fallback (if the built-in integration can’t determine your editor URL)\nIf your setup is non-standard (custom routing, headless frontends, etc.), use the **Meta tag format** below to provide Silktide with an explicit `editorUrl` per page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "jadu",
    "name": "Jadu",
    "category": "Public sector CMS",
    "silktide_support": "built_in",
    "silktide_help_url": "https://help.silktide.com/en/articles/8020846-how-to-integrate-silktide-with-jadu",
    "edit_link_strategy": "built_in",
    "retest_strategy": "built_in",
    "notes": "**Deep-link support:** Built-in (Silktide handles the Edit button).\n**What to do:** Follow the Silktide integration guide in `silktide_help_url`.\n**Editor experience:** Users click **Edit** in Silktide and are taken to the correct item in the CMS.",
    "notes_original": "Built-in Jadu integration.",
    "editor_context": {
      "strategy": "built_in",
      "silktide_support": "built_in",
      "primary_docs": "https://help.silktide.com/en/articles/8020846-how-to-integrate-silktide-with-jadu"
    },
    "template_md": "# Jadu → Silktide deep-link integration template\n\n**Silktide support:** Built-in (Silktide)  \n**Recommended deep-link approach:** Built-in integration\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Jadu**.\n\n## CMS-specific considerations\nBuilt-in Jadu integration.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Silktide’s built-in integration\nFollow the Silktide Help Center guide for **Jadu**: https://help.silktide.com/en/articles/8020846-how-to-integrate-silktide-with-jadu\nOnce configured, Silktide should show an **Edit** button that opens the correct editor screen.\n\n### Fallback (if the built-in integration can’t determine your editor URL)\nIf your setup is non-standard (custom routing, headless frontends, etc.), use the **Meta tag format** below to provide Silktide with an explicit `editorUrl` per page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "cascade_cms",
    "name": "Cascade CMS (Hannon Hill)",
    "category": "Enterprise / Higher-Ed CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Site template/header include used across all pages.\n**How to build `editorUrl`:** Cascade authoring is asset-ID driven; store/emit the relevant asset ID for the published page.\n**Common editor URL pattern(s):** https://{CASCADE_HOST}/entity/open.act?id={ASSET_ID}&type=page (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Cascade editor URLs are asset-ID based; public URLs typically do not include the internal asset ID, so Silktide cannot reliably derive editor links via URL transform. Output a per-page silktide-cms meta tag containing the correct editorUrl.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{CASCADE_HOST}/entity/open.act?id={ASSET_ID}&type=page"
      ],
      "id_source_hint": "Cascade authoring is asset-ID driven; store/emit the relevant asset ID for the published page.",
      "where_to_inject": "Site template/header include used across all pages."
    },
    "template_md": "# Cascade CMS (Hannon Hill) → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag per page\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Cascade CMS (Hannon Hill)**.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Provide the editor URL via a per-page meta tag\nCascade editor URLs are asset-ID based, and the public URL usually doesn’t contain that internal ID. For reliable deep links, output a **silktide-cms** meta tag on each page with the exact `editorUrl` for that page.\n\n**Common editor URL pattern (varies by instance):**\n`https://CASCADE_HOST/entity/open.act?id=ASSET_ID&type=page`\n\n### Where to implement\nAdd the meta tag in the shared site template/header include so it renders on every page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"Cascade CMS\", \"editorUrl\": \"https://cms.example.com/entity/open.act?id=...&type=page\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (asset IDs), generate this tag dynamically at render time.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "typo3",
    "name": "TYPO3",
    "category": "Open-source CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "hook_or_extension",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Global frontend <head> (Fluid layout / sitepackage) so every page emits its own editorUrl.\n**How to build `editorUrl`:** Use the current page UID from TYPO3 during render (page tree node UID).\n**Common editor URL pattern(s):** https://{SITE}/typo3/record/edit?edit[pages][{PAGE_UID}]=edit (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "TYPO3 backend deep links include internal page uid and often a security token/route; public URL alone isn’t enough. Use meta tag built from page uid + backend route.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{SITE}/typo3/record/edit?edit[pages][{PAGE_UID}]=edit"
      ],
      "id_source_hint": "Use the current page UID from TYPO3 during render (page tree node UID).",
      "where_to_inject": "Global frontend <head> (Fluid layout / sitepackage) so every page emits its own editorUrl."
    },
    "template_md": "# TYPO3 → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **TYPO3**.\n\n## CMS-specific considerations\nTYPO3 backend deep links include internal page uid and often a security token/route; public URL alone isn’t enough. Use meta tag built from page uid + backend route.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "joomla",
    "name": "Joomla",
    "category": "Open-source CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "plugin",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Template <head> override (or system plugin) to emit the meta tag per page.\n**How to build `editorUrl`:** If the page is an article, use the article ID; for menu items/components, map to the underlying entity ID.\n**Common editor URL pattern(s):** https://{SITE}/administrator/index.php?option=com_content&task=article.edit&id={ARTICLE_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Use a per-page silktide-cms meta tag to provide the exact editor URL for each page.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{SITE}/administrator/index.php?option=com_content&task=article.edit&id={ARTICLE_ID}"
      ],
      "id_source_hint": "If the page is an article, use the article ID; for menu items/components, map to the underlying entity ID.",
      "where_to_inject": "Template <head> override (or system plugin) to emit the meta tag per page."
    },
    "template_md": "# Joomla → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Joomla**.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "shopify",
    "name": "Shopify",
    "category": "Ecommerce (SaaS)",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** theme.liquid <head> (or equivalent layout) with logic per template type.\n**How to build `editorUrl`:** In Liquid, many templates expose object IDs (e.g., product.id, page.id, article.id). Use the relevant one.\n**Common editor URL pattern(s):** https://admin.shopify.com/store/{STORE_HANDLE}/pages/{PAGE_ID}; https://admin.shopify.com/store/{STORE_HANDLE}/products/{PRODUCT_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Admin URLs are ID-based; in Liquid you can access object.id for products/pages/blog posts and build the editorUrl meta tag.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://admin.shopify.com/store/{STORE_HANDLE}/pages/{PAGE_ID}",
        "https://admin.shopify.com/store/{STORE_HANDLE}/products/{PRODUCT_ID}",
        "https://admin.shopify.com/store/{STORE_HANDLE}/blogs/{BLOG_ID}/articles/{ARTICLE_ID}"
      ],
      "id_source_hint": "In Liquid, many templates expose object IDs (e.g., product.id, page.id, article.id). Use the relevant one.",
      "where_to_inject": "theme.liquid <head> (or equivalent layout) with logic per template type."
    },
    "template_md": "# Shopify → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Shopify**.\n\n## CMS-specific considerations\nAdmin URLs are ID-based; in Liquid you can access object.id for products/pages/blog posts and build the editorUrl meta tag.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "magento",
    "name": "Adobe Commerce (Magento)",
    "category": "Ecommerce",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "module_event",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Frontend theme head block; compute editorUrl server-side.\n**How to build `editorUrl`:** Use entity IDs from Magento models (product ID, CMS page ID). Avoid including form keys/tokens.\n**Common editor URL pattern(s):** https://{SITE}/admin/*/catalog/product/edit/id/{PRODUCT_ID}; https://{SITE}/admin/*/cms/page/edit/page_id/{PAGE_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Admin edit screens use entity IDs and often require an authenticated session; do not include tokens in editor URLs. Use meta tag with non-tokenized edit URL.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{SITE}/admin/*/catalog/product/edit/id/{PRODUCT_ID}",
        "https://{SITE}/admin/*/cms/page/edit/page_id/{PAGE_ID}"
      ],
      "id_source_hint": "Use entity IDs from Magento models (product ID, CMS page ID). Avoid including form keys/tokens.",
      "where_to_inject": "Frontend theme head block; compute editorUrl server-side."
    },
    "template_md": "# Adobe Commerce (Magento) → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Adobe Commerce (Magento)**.\n\n## CMS-specific considerations\nAdmin edit screens use entity IDs and often require an authenticated session; do not include tokens in editor URLs. Use meta tag with non-tokenized edit URL.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "bigcommerce",
    "name": "BigCommerce",
    "category": "Ecommerce (SaaS)",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Storefront theme <head> (Stencil) with context-aware variables.\n**How to build `editorUrl`:** Use the internal object ID if available in templates; if not, you'll need a mapping from URL/slug to ID.\n**Common editor URL pattern(s):** https://store-{STORE_HASH}.mybigcommerce.com/manage/products/{PRODUCT_ID}; https://store-{STORE_HASH}.mybigcommerce.com/manage/pages/{PAGE_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Admin edit screens are ID-based. Use storefront templates to emit meta tag if you can access product/page IDs; otherwise rely on URL transform is not recommended.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://store-{STORE_HASH}.mybigcommerce.com/manage/products/{PRODUCT_ID}",
        "https://store-{STORE_HASH}.mybigcommerce.com/manage/pages/{PAGE_ID}"
      ],
      "id_source_hint": "Use the internal object ID if available in templates; if not, you'll need a mapping from URL/slug to ID.",
      "where_to_inject": "Storefront theme <head> (Stencil) with context-aware variables."
    },
    "template_md": "# BigCommerce → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **BigCommerce**.\n\n## CMS-specific considerations\nAdmin edit screens are ID-based. Use storefront templates to emit meta tag if you can access product/page IDs; otherwise rely on URL transform is not recommended.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "webflow",
    "name": "Webflow",
    "category": "Website builder",
    "silktide_support": "custom",
    "edit_link_strategy": "not_feasible",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Not feasible (no stable page-specific editor URL).\n**Recommendation:** Link to the CMS dashboard or relevant content list, or omit Edit links for this CMS.\n**Context:** Webflow editing URLs are not reliably derivable from public URLs without internal IDs; deep linking to a specific page is typically not feasible.",
    "notes_original": "Webflow editing URLs are not reliably derivable from public URLs without internal IDs; deep linking to a specific page is typically not feasible.",
    "editor_context": {
      "strategy": "not_feasible",
      "silktide_support": "custom"
    },
    "template_md": "# Webflow → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Not feasible by URL transform (meta tag only if you can compute editor URLs)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Webflow**.\n\n## CMS-specific considerations\nWebflow editing URLs are not reliably derivable from public URLs without internal IDs; deep linking to a specific page is typically not feasible.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### URL transform is not reliable for this platform\nFor most deployments, the editor URL cannot be derived from the public URL in a deterministic way.\nIf you can compute an editor URL per page (e.g., via a mapping layer), use the **Meta tag format** below.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "wix",
    "name": "Wix",
    "category": "Website builder",
    "silktide_support": "custom",
    "edit_link_strategy": "not_feasible",
    "retest_strategy": "limited",
    "notes": "**Deep-link approach:** Not feasible (no stable page-specific editor URL).\n**Recommendation:** Link to the CMS dashboard or relevant content list, or omit Edit links for this CMS.\n**Context:** Deep linking to edit a specific page is generally not feasible; Wix uses editor/session context. Consider linking to the site dashboard only, or skip edit links.",
    "notes_original": "Deep linking to edit a specific page is generally not feasible; Wix uses editor/session context. Consider linking to the site dashboard only, or skip edit links.",
    "editor_context": {
      "strategy": "not_feasible",
      "silktide_support": "custom"
    },
    "template_md": "# Wix → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Not feasible by URL transform (meta tag only if you can compute editor URLs)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Wix**.\n\n## CMS-specific considerations\nDeep linking to edit a specific page is generally not feasible; Wix uses editor/session context. Consider linking to the site dashboard only, or skip edit links.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### URL transform is not reliable for this platform\nFor most deployments, the editor URL cannot be derived from the public URL in a deterministic way.\nIf you can compute an editor URL per page (e.g., via a mapping layer), use the **Meta tag format** below.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "squarespace",
    "name": "Squarespace",
    "category": "Website builder",
    "silktide_support": "custom",
    "edit_link_strategy": "not_feasible",
    "retest_strategy": "limited",
    "notes": "**Deep-link approach:** Not feasible (no stable page-specific editor URL).\n**Recommendation:** Link to the CMS dashboard or relevant content list, or omit Edit links for this CMS.\n**Context:** Page-specific edit deep links are generally not feasible; editor URLs are not stable per page. Consider linking to the site dashboard only, or skip edit links.",
    "notes_original": "Page-specific edit deep links are generally not feasible; editor URLs are not stable per page. Consider linking to the site dashboard only, or skip edit links.",
    "editor_context": {
      "strategy": "not_feasible",
      "silktide_support": "custom"
    },
    "template_md": "# Squarespace → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Not feasible by URL transform (meta tag only if you can compute editor URLs)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Squarespace**.\n\n## CMS-specific considerations\nPage-specific edit deep links are generally not feasible; editor URLs are not stable per page. Consider linking to the site dashboard only, or skip edit links.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### URL transform is not reliable for this platform\nFor most deployments, the editor URL cannot be derived from the public URL in a deterministic way.\nIf you can compute an editor URL per page (e.g., via a mapping layer), use the **Meta tag format** below.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "hubspot",
    "name": "HubSpot CMS",
    "category": "SaaS CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** HubSpot theme templates (<head>) or module that renders on all pages.\n**How to build `editorUrl`:** Needs portalId + contentId; if templates can access them, emit directly; otherwise you may need a lookup/mapping layer.\n**Common editor URL pattern(s):** https://app.hubspot.com/content/{PORTAL_ID}/edit/{CONTENT_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "HubSpot editor URLs include portal ID and content ID. If your templates can access these IDs, emit meta tag; otherwise edit deep links may be limited.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://app.hubspot.com/content/{PORTAL_ID}/edit/{CONTENT_ID}"
      ],
      "id_source_hint": "Needs portalId + contentId; if templates can access them, emit directly; otherwise you may need a lookup/mapping layer.",
      "where_to_inject": "HubSpot theme templates (<head>) or module that renders on all pages."
    },
    "template_md": "# HubSpot CMS → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **HubSpot CMS**.\n\n## CMS-specific considerations\nHubSpot editor URLs include portal ID and content ID. If your templates can access these IDs, emit meta tag; otherwise edit deep links may be limited.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "sitecore",
    "name": "Sitecore",
    "category": "Enterprise DXP",
    "silktide_support": "custom",
    "edit_link_strategy": "url_transform",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** URL transform (Silktide derives the editor URL by manipulating the public URL).\n**Transform rule:** Commonly add `sc_mode=edit` (and sometimes `sc_lang`/`sc_site`) or use an Experience Editor host mapping.\n**Notes:** Varies by Sitecore version and routing; if the editor requires item IDs, use meta tag instead.\n**When to use meta tag instead:** If the CMS editor URL requires internal IDs (e.g., pageId/itemId) that are not present in the public URL.",
    "notes_original": "Many Sitecore sites support Experience Editor via predictable URL changes (e.g., query params like sc_mode=edit and/or host swap). If your setup requires item IDs, use the meta tag strategy.",
    "editor_context": {
      "strategy": "url_transform",
      "silktide_support": "custom",
      "transform_rule": "Commonly add `sc_mode=edit` (and sometimes `sc_lang`/`sc_site`) or use an Experience Editor host mapping.",
      "limitations": "Varies by Sitecore version and routing; if the editor requires item IDs, use meta tag instead."
    },
    "template_md": "# Sitecore → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** URL transform (derive editor URL from public URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Sitecore**.\n\n## CMS-specific considerations\nMany Sitecore sites support Experience Editor via predictable URL changes (e.g., query params like sc_mode=edit and/or host swap). If your setup requires item IDs, use the meta tag strategy.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use URL transform (recommended)\nIf your CMS editor URL can be derived from the public URL **without internal IDs** (for example: host swap, append `/edit`, or add an edit query parameter), Silktide can deep-link using a URL transform.\nIf the editor URL requires internal IDs (e.g., `pageId=123`), use the **Meta tag format** below instead.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## URL transform (optional)\n\nIf your CMS editor URL can be derived from the public URL using a deterministic string rule (**without internal IDs**), Silktide can deep-link via URL transforms.\n\n**Examples of common transforms**\n- **Host swap:** `https://www.example.com/path` → `https://edit.example.com/path`\n- **Path prefix:** `https://www.example.com/path` → `https://www.example.com/admin/edit/path`\n- **Append segment:** `https://www.example.com/path` → `https://www.example.com/path/edit`\n- **Add query param:** `https://www.example.com/path` → `https://www.example.com/path?mode=edit`\n\n**What to provide**\n- 2–3 example public URLs\n- The matching editor URLs\n- The exact transform rule(s)\n\nIf the editor URL requires IDs not present in the public URL (e.g., `pageId=123`), URL transform will not work—use the **Meta tag format** above.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "optimizely",
    "name": "Optimizely CMS",
    "category": "Enterprise CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "depends",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Depends (URL transform may work in some setups; meta tag is the safe default).\n**Decision rule:** If you can deterministically map the public URL to an editor URL without internal IDs, use URL transform. Otherwise use the meta tag.\n**Context:** Optimizely supports on-page editing; some implementations use epieditmode=True on specific CMS URLs (often includes internal IDs). Treat as meta tag by default, but URL transform may work if your preview URLs are deterministic.",
    "notes_original": "Optimizely supports on-page editing; some implementations use epieditmode=True on specific CMS URLs (often includes internal IDs). Treat as meta tag by default, but URL transform may work if your preview URLs are deterministic.",
    "editor_context": {
      "strategy": "depends",
      "silktide_support": "custom"
    },
    "template_md": "# Optimizely CMS → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Depends (transform if possible, otherwise meta tag)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Optimizely CMS**.\n\n## CMS-specific considerations\nOptimizely supports on-page editing; some implementations use epieditmode=True on specific CMS URLs (often includes internal IDs). Treat as meta tag by default, but URL transform may work if your preview URLs are deterministic.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Prefer URL transform if your setup supports it\nIf your editor URL is derivable from the public URL without IDs, use **URL transform**.\nIf not (common), use the **Meta tag format** below.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## URL transform (optional)\n\nIf your CMS editor URL can be derived from the public URL using a deterministic string rule (**without internal IDs**), Silktide can deep-link via URL transforms.\n\n**Examples of common transforms**\n- **Host swap:** `https://www.example.com/path` → `https://edit.example.com/path`\n- **Path prefix:** `https://www.example.com/path` → `https://www.example.com/admin/edit/path`\n- **Append segment:** `https://www.example.com/path` → `https://www.example.com/path/edit`\n- **Add query param:** `https://www.example.com/path` → `https://www.example.com/path?mode=edit`\n\n**What to provide**\n- 2–3 example public URLs\n- The matching editor URLs\n- The exact transform rule(s)\n\nIf the editor URL requires IDs not present in the public URL (e.g., `pageId=123`), URL transform will not work—use the **Meta tag format** above.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "kentico",
    "name": "Kentico Xperience",
    "category": "Enterprise CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "depends",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Depends (URL transform may work in some setups; meta tag is the safe default).\n**Decision rule:** If you can deterministically map the public URL to an editor URL without internal IDs, use URL transform. Otherwise use the meta tag.\n**Context:** Often requires internal identifiers (NodeID/DocumentID). If on-site editing is enabled, some setups allow an edit-mode URL derived from the public path (e.g., /cmsctx/... + editmode=1). Otherwise use meta tag built from CMS IDs.",
    "notes_original": "Often requires internal identifiers (NodeID/DocumentID). If on-site editing is enabled, some setups allow an edit-mode URL derived from the public path (e.g., /cmsctx/... + editmode=1). Otherwise use meta tag built from CMS IDs.",
    "editor_context": {
      "strategy": "depends",
      "silktide_support": "custom"
    },
    "template_md": "# Kentico Xperience → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Depends (transform if possible, otherwise meta tag)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Kentico Xperience**.\n\n## CMS-specific considerations\nOften requires internal identifiers (NodeID/DocumentID). If on-site editing is enabled, some setups allow an edit-mode URL derived from the public path (e.g., /cmsctx/... + editmode=1). Otherwise use meta tag built from CMS IDs.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Prefer URL transform if your setup supports it\nIf your editor URL is derivable from the public URL without IDs, use **URL transform**.\nIf not (common), use the **Meta tag format** below.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## URL transform (optional)\n\nIf your CMS editor URL can be derived from the public URL using a deterministic string rule (**without internal IDs**), Silktide can deep-link via URL transforms.\n\n**Examples of common transforms**\n- **Host swap:** `https://www.example.com/path` → `https://edit.example.com/path`\n- **Path prefix:** `https://www.example.com/path` → `https://www.example.com/admin/edit/path`\n- **Append segment:** `https://www.example.com/path` → `https://www.example.com/path/edit`\n- **Add query param:** `https://www.example.com/path` → `https://www.example.com/path?mode=edit`\n\n**What to provide**\n- 2–3 example public URLs\n- The matching editor URLs\n- The exact transform rule(s)\n\nIf the editor URL requires IDs not present in the public URL (e.g., `pageId=123`), URL transform will not work—use the **Meta tag format** above.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "sharepoint",
    "name": "SharePoint Online",
    "category": "Intranet / DXP",
    "silktide_support": "custom",
    "edit_link_strategy": "url_transform",
    "retest_strategy": "power_automate_or_webhook",
    "notes": "**Deep-link approach:** URL transform (Silktide derives the editor URL by manipulating the public URL).\n**Transform rule:** Tenant/page-type dependent. Where supported, use an edit-mode parameter (e.g., `?Mode=Edit`) or link to the Pages library edit UI.\n**Notes:** SharePoint URL shapes vary; if edit-mode URLs aren't stable, link to the Pages library or use meta tag if you can inject it.\n**When to use meta tag instead:** If the CMS editor URL requires internal IDs (e.g., pageId/itemId) that are not present in the public URL.",
    "notes_original": "Some SharePoint Online pages can be opened in edit mode via predictable URL parameters (varies by tenant/page type). If not, fall back to meta tag with internal IDs or link to the site’s Pages/Library UI.",
    "editor_context": {
      "strategy": "url_transform",
      "silktide_support": "custom",
      "transform_rule": "Tenant/page-type dependent. Where supported, use an edit-mode parameter (e.g., `?Mode=Edit`) or link to the Pages library edit UI.",
      "limitations": "SharePoint URL shapes vary; if edit-mode URLs aren't stable, link to the Pages library or use meta tag if you can inject it."
    },
    "template_md": "# SharePoint Online → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** URL transform (derive editor URL from public URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **SharePoint Online**.\n\n## CMS-specific considerations\nSome SharePoint Online pages can be opened in edit mode via predictable URL parameters (varies by tenant/page type). If not, fall back to meta tag with internal IDs or link to the site’s Pages/Library UI.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use URL transform (recommended)\nIf your CMS editor URL can be derived from the public URL **without internal IDs** (for example: host swap, append `/edit`, or add an edit query parameter), Silktide can deep-link using a URL transform.\nIf the editor URL requires internal IDs (e.g., `pageId=123`), use the **Meta tag format** below instead.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## URL transform (optional)\n\nIf your CMS editor URL can be derived from the public URL using a deterministic string rule (**without internal IDs**), Silktide can deep-link via URL transforms.\n\n**Examples of common transforms**\n- **Host swap:** `https://www.example.com/path` → `https://edit.example.com/path`\n- **Path prefix:** `https://www.example.com/path` → `https://www.example.com/admin/edit/path`\n- **Append segment:** `https://www.example.com/path` → `https://www.example.com/path/edit`\n- **Add query param:** `https://www.example.com/path` → `https://www.example.com/path?mode=edit`\n\n**What to provide**\n- 2–3 example public URLs\n- The matching editor URLs\n- The exact transform rule(s)\n\nIf the editor URL requires IDs not present in the public URL (e.g., `pageId=123`), URL transform will not work—use the **Meta tag format** above.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "liferay",
    "name": "Liferay DXP",
    "category": "Enterprise DXP",
    "silktide_support": "custom",
    "edit_link_strategy": "url_transform",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** URL transform (Silktide derives the editor URL by manipulating the public URL).\n**Transform rule:** Add `p_l_mode=edit` to the page URL query string (e.g., `...?p_l_mode=edit`).\n**Notes:** Requires permissions; portals may still accept this even with friendly URLs.\n**When to use meta tag instead:** If the CMS editor URL requires internal IDs (e.g., pageId/itemId) that are not present in the public URL.",
    "notes_original": "Liferay pages can be opened in edit mode by adding p_l_mode=edit to the page URL (requires permissions).",
    "editor_context": {
      "strategy": "url_transform",
      "silktide_support": "custom",
      "transform_rule": "Add `p_l_mode=edit` to the page URL query string (e.g., `...?p_l_mode=edit`).",
      "limitations": "Requires permissions; portals may still accept this even with friendly URLs."
    },
    "template_md": "# Liferay DXP → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** URL transform (derive editor URL from public URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Liferay DXP**.\n\n## CMS-specific considerations\nLiferay pages can be opened in edit mode by adding p_l_mode=edit to the page URL (requires permissions).\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use URL transform (recommended)\nIf your CMS editor URL can be derived from the public URL **without internal IDs** (for example: host swap, append `/edit`, or add an edit query parameter), Silktide can deep-link using a URL transform.\nIf the editor URL requires internal IDs (e.g., `pageId=123`), use the **Meta tag format** below instead.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## URL transform (optional)\n\nIf your CMS editor URL can be derived from the public URL using a deterministic string rule (**without internal IDs**), Silktide can deep-link via URL transforms.\n\n**Examples of common transforms**\n- **Host swap:** `https://www.example.com/path` → `https://edit.example.com/path`\n- **Path prefix:** `https://www.example.com/path` → `https://www.example.com/admin/edit/path`\n- **Append segment:** `https://www.example.com/path` → `https://www.example.com/path/edit`\n- **Add query param:** `https://www.example.com/path` → `https://www.example.com/path?mode=edit`\n\n**What to provide**\n- 2–3 example public URLs\n- The matching editor URLs\n- The exact transform rule(s)\n\nIf the editor URL requires IDs not present in the public URL (e.g., `pageId=123`), URL transform will not work—use the **Meta tag format** above.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "magnolia",
    "name": "Magnolia",
    "category": "Enterprise CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** In the global frontend `<head>` so every page emits a `silktide-cms` meta tag.\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Typically edited via AdminCentral/Pages app and internal node paths/UUIDs; no universal 1:1 URL transform from public URL. Use meta tag built from Magnolia page node info.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom"
    },
    "template_md": "# Magnolia → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Magnolia**.\n\n## CMS-specific considerations\nTypically edited via AdminCentral/Pages app and internal node paths/UUIDs; no universal 1:1 URL transform from public URL. Use meta tag built from Magnolia page node info.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "contentful",
    "name": "Contentful",
    "category": "Headless CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Your frontend app layout/head; can be server-rendered or injected at build time.\n**How to build `editorUrl`:** Emit the Contentful entry ID used to render the page (plus space/environment).\n**Common editor URL pattern(s):** https://app.contentful.com/spaces/{SPACE_ID}/environments/{ENV_ID}/entries/{ENTRY_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Headless: frontend can emit meta tag with the entry ID and a preconfigured space/environment.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://app.contentful.com/spaces/{SPACE_ID}/environments/{ENV_ID}/entries/{ENTRY_ID}"
      ],
      "id_source_hint": "Emit the Contentful entry ID used to render the page (plus space/environment).",
      "where_to_inject": "Your frontend app layout/head; can be server-rendered or injected at build time."
    },
    "template_md": "# Contentful → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Contentful**.\n\n## CMS-specific considerations\nHeadless: frontend can emit meta tag with the entry ID and a preconfigured space/environment.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "sanity",
    "name": "Sanity",
    "category": "Headless CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Frontend head; many setups already have document data at render time.\n**How to build `editorUrl`:** Emit the Sanity document _id (and optionally _type) used for the page; studio host is customer-specific.\n**Common editor URL pattern(s):** https://{STUDIO_HOST}/intent/edit/id={DOC_ID};type={DOC_TYPE} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Headless: emit meta tag with document ID and studio URL (often uses intents).",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{STUDIO_HOST}/intent/edit/id={DOC_ID};type={DOC_TYPE}"
      ],
      "id_source_hint": "Emit the Sanity document _id (and optionally _type) used for the page; studio host is customer-specific.",
      "where_to_inject": "Frontend head; many setups already have document data at render time."
    },
    "template_md": "# Sanity → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Sanity**.\n\n## CMS-specific considerations\nHeadless: emit meta tag with document ID and studio URL (often uses intents).\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "prismic",
    "name": "Prismic",
    "category": "Headless CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Frontend head in the app that renders Prismic content.\n**How to build `editorUrl`:** Emit the Prismic document ID (or UID) used for the page; repo name is customer-specific.\n**Common editor URL pattern(s):** https://{REPO}.prismic.io/documents~b=working&documentId={DOC_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Headless: emit meta tag with document ID/UID and repository name.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{REPO}.prismic.io/documents~b=working&documentId={DOC_ID}"
      ],
      "id_source_hint": "Emit the Prismic document ID (or UID) used for the page; repo name is customer-specific.",
      "where_to_inject": "Frontend head in the app that renders Prismic content."
    },
    "template_md": "# Prismic → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Prismic**.\n\n## CMS-specific considerations\nHeadless: emit meta tag with document ID/UID and repository name.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "storyblok",
    "name": "Storyblok",
    "category": "Headless CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Frontend head; Storyblok SDK already passes story info in preview modes.\n**How to build `editorUrl`:** Emit Story ID and Space ID; Storyblok content APIs usually provide story.id.\n**Common editor URL pattern(s):** https://app.storyblok.com/#/me/spaces/{SPACE_ID}/stories/0/0/{STORY_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Headless: emit meta tag with story ID and space ID, pointing into the Visual Editor.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://app.storyblok.com/#/me/spaces/{SPACE_ID}/stories/0/0/{STORY_ID}"
      ],
      "id_source_hint": "Emit Story ID and Space ID; Storyblok content APIs usually provide story.id.",
      "where_to_inject": "Frontend head; Storyblok SDK already passes story info in preview modes."
    },
    "template_md": "# Storyblok → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Storyblok**.\n\n## CMS-specific considerations\nHeadless: emit meta tag with story ID and space ID, pointing into the Visual Editor.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "strapi",
    "name": "Strapi",
    "category": "Headless CMS (self-hosted)",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "lifecycle_hook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Frontend head (SSR/templating) once you know which entry rendered the page.\n**How to build `editorUrl`:** Emit the collection type and entry ID used for rendering; admin host may differ from public host.\n**Common editor URL pattern(s):** https://{ADMIN_HOST}/admin/content-manager/collectionType/api::{API_NAME}.{TYPE}/{ENTRY_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Emit meta tag with collection type + entry ID and Strapi admin URL.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{ADMIN_HOST}/admin/content-manager/collectionType/api::{API_NAME}.{TYPE}/{ENTRY_ID}"
      ],
      "id_source_hint": "Emit the collection type and entry ID used for rendering; admin host may differ from public host.",
      "where_to_inject": "Frontend head (SSR/templating) once you know which entry rendered the page."
    },
    "template_md": "# Strapi → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Strapi**.\n\n## CMS-specific considerations\nEmit meta tag with collection type + entry ID and Strapi admin URL.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "contentstack",
    "name": "Contentstack",
    "category": "Headless CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Frontend head in your site/app.\n**How to build `editorUrl`:** Emit stack UID + content type UID + entry UID used for the page.\n**Common editor URL pattern(s):** https://app.contentstack.com/#!/stack/{STACK_UID}/content-types/{CT_UID}/entries/{ENTRY_UID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Use a per-page silktide-cms meta tag to provide the exact editor URL for each page.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://app.contentstack.com/#!/stack/{STACK_UID}/content-types/{CT_UID}/entries/{ENTRY_UID}"
      ],
      "id_source_hint": "Emit stack UID + content type UID + entry UID used for the page.",
      "where_to_inject": "Frontend head in your site/app."
    },
    "template_md": "# Contentstack → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Contentstack**.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "ghost",
    "name": "Ghost",
    "category": "CMS (Publishing)",
    "silktide_support": "custom",
    "edit_link_strategy": "url_transform",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** URL transform (Silktide derives the editor URL by manipulating the public URL).\n**Transform rule:** Append `/edit` to the public URL path (e.g., `https://site.com/post` → `https://site.com/post/edit`).\n**Notes:** Requires the user to be logged in; Ghost may redirect to login first.\n**When to use meta tag instead:** If the CMS editor URL requires internal IDs (e.g., pageId/itemId) that are not present in the public URL.",
    "notes_original": "Ghost supports a front-end “/edit” shortcut: append /edit to the public post/page URL to redirect to the editor (login required).",
    "editor_context": {
      "strategy": "url_transform",
      "silktide_support": "custom",
      "transform_rule": "Append `/edit` to the public URL path (e.g., `https://site.com/post` → `https://site.com/post/edit`).",
      "limitations": "Requires the user to be logged in; Ghost may redirect to login first."
    },
    "template_md": "# Ghost → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** URL transform (derive editor URL from public URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Ghost**.\n\n## CMS-specific considerations\nGhost supports a front-end “/edit” shortcut: append /edit to the public post/page URL to redirect to the editor (login required).\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use URL transform (recommended)\nIf your CMS editor URL can be derived from the public URL **without internal IDs** (for example: host swap, append `/edit`, or add an edit query parameter), Silktide can deep-link using a URL transform.\nIf the editor URL requires internal IDs (e.g., `pageId=123`), use the **Meta tag format** below instead.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## URL transform (optional)\n\nIf your CMS editor URL can be derived from the public URL using a deterministic string rule (**without internal IDs**), Silktide can deep-link via URL transforms.\n\n**Examples of common transforms**\n- **Host swap:** `https://www.example.com/path` → `https://edit.example.com/path`\n- **Path prefix:** `https://www.example.com/path` → `https://www.example.com/admin/edit/path`\n- **Append segment:** `https://www.example.com/path` → `https://www.example.com/path/edit`\n- **Add query param:** `https://www.example.com/path` → `https://www.example.com/path?mode=edit`\n\n**What to provide**\n- 2–3 example public URLs\n- The matching editor URLs\n- The exact transform rule(s)\n\nIf the editor URL requires IDs not present in the public URL (e.g., `pageId=123`), URL transform will not work—use the **Meta tag format** above.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "woocommerce",
    "name": "WooCommerce",
    "category": "Ecommerce (WordPress plugin)",
    "silktide_support": "built_in",
    "silktide_help_url": "https://help.silktide.com/en/articles/8117667-how-to-integrate-silktide-with-wordpress",
    "edit_link_strategy": "built_in",
    "retest_strategy": "built_in",
    "notes": "**Deep-link support:** Built-in (Silktide handles the Edit button).\n**What to do:** Follow the Silktide integration guide in `silktide_help_url`.\n**Editor experience:** Users click **Edit** in Silktide and are taken to the correct item in the CMS.",
    "notes_original": "Silktide provides a built-in integration for deep-linking (Edit button).",
    "editor_context": {
      "strategy": "built_in",
      "silktide_support": "built_in",
      "primary_docs": "https://help.silktide.com/en/articles/8117667-how-to-integrate-silktide-with-wordpress"
    },
    "template_md": "# WooCommerce → Silktide deep-link integration template\n\n**Silktide support:** Built-in (Silktide)  \n**Recommended deep-link approach:** Built-in integration\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **WooCommerce**.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Silktide’s built-in integration\nFollow the Silktide Help Center guide for **WooCommerce**: https://help.silktide.com/en/articles/8117667-how-to-integrate-silktide-with-wordpress\nOnce configured, Silktide should show an **Edit** button that opens the correct editor screen.\n\n### Fallback (if the built-in integration can’t determine your editor URL)\nIf your setup is non-standard (custom routing, headless frontends, etc.), use the **Meta tag format** below to provide Silktide with an explicit `editorUrl` per page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "prestashop",
    "name": "PrestaShop",
    "category": "Open-source ecommerce",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Theme header template with per-page variables.\n**How to build `editorUrl`:** Use internal IDs (product ID, CMS page ID). Avoid any security tokens in the URL.\n**Common editor URL pattern(s):** https://{SITE}/admin*/index.php?controller=AdminProducts&id_product={PRODUCT_ID}&updateproduct=1; https://{SITE}/admin*/index.php?controller=AdminCmsContent&id_cms={CMS_PAGE_ID}&updatecms=1 (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Admin edit URLs typically require internal IDs (product/category/CMS page).",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{SITE}/admin*/index.php?controller=AdminProducts&id_product={PRODUCT_ID}&updateproduct=1",
        "https://{SITE}/admin*/index.php?controller=AdminCmsContent&id_cms={CMS_PAGE_ID}&updatecms=1"
      ],
      "id_source_hint": "Use internal IDs (product ID, CMS page ID). Avoid any security tokens in the URL.",
      "where_to_inject": "Theme header template with per-page variables."
    },
    "template_md": "# PrestaShop → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **PrestaShop**.\n\n## CMS-specific considerations\nAdmin edit URLs typically require internal IDs (product/category/CMS page).\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "shopware",
    "name": "Shopware",
    "category": "Ecommerce platform",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Storefront head; requires access to entity IDs in template context.\n**How to build `editorUrl`:** Use Shopware entity IDs (often UUIDs) for products/categories/pages.\n**Common editor URL pattern(s):** https://{ADMIN_HOST}/admin#/sw/product/detail/{PRODUCT_ID}; https://{ADMIN_HOST}/admin#/sw/category/detail/{CATEGORY_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Admin edit URLs usually include entity IDs; public slugs aren’t enough.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{ADMIN_HOST}/admin#/sw/product/detail/{PRODUCT_ID}",
        "https://{ADMIN_HOST}/admin#/sw/category/detail/{CATEGORY_ID}"
      ],
      "id_source_hint": "Use Shopware entity IDs (often UUIDs) for products/categories/pages.",
      "where_to_inject": "Storefront head; requires access to entity IDs in template context."
    },
    "template_md": "# Shopware → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Shopware**.\n\n## CMS-specific considerations\nAdmin edit URLs usually include entity IDs; public slugs aren’t enough.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "plone",
    "name": "Plone",
    "category": "Open-source CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "url_transform",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** URL transform (Silktide derives the editor URL by manipulating the public URL).\n**Transform rule:** Append `/edit` to the public URL (e.g., `https://site.com/page` → `https://site.com/page/edit`).\n**Notes:** Requires editor permissions; some sites may customize actions/paths.\n**When to use meta tag instead:** If the CMS editor URL requires internal IDs (e.g., pageId/itemId) that are not present in the public URL.",
    "notes_original": "Plone supports action URLs like /edit on the same content URL, so editorUrl can be derived from the public URL.",
    "editor_context": {
      "strategy": "url_transform",
      "silktide_support": "custom",
      "transform_rule": "Append `/edit` to the public URL (e.g., `https://site.com/page` → `https://site.com/page/edit`).",
      "limitations": "Requires editor permissions; some sites may customize actions/paths."
    },
    "template_md": "# Plone → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** URL transform (derive editor URL from public URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Plone**.\n\n## CMS-specific considerations\nPlone supports action URLs like /edit on the same content URL, so editorUrl can be derived from the public URL.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use URL transform (recommended)\nIf your CMS editor URL can be derived from the public URL **without internal IDs** (for example: host swap, append `/edit`, or add an edit query parameter), Silktide can deep-link using a URL transform.\nIf the editor URL requires internal IDs (e.g., `pageId=123`), use the **Meta tag format** below instead.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## URL transform (optional)\n\nIf your CMS editor URL can be derived from the public URL using a deterministic string rule (**without internal IDs**), Silktide can deep-link via URL transforms.\n\n**Examples of common transforms**\n- **Host swap:** `https://www.example.com/path` → `https://edit.example.com/path`\n- **Path prefix:** `https://www.example.com/path` → `https://www.example.com/admin/edit/path`\n- **Append segment:** `https://www.example.com/path` → `https://www.example.com/path/edit`\n- **Add query param:** `https://www.example.com/path` → `https://www.example.com/path?mode=edit`\n\n**What to provide**\n- 2–3 example public URLs\n- The matching editor URLs\n- The exact transform rule(s)\n\nIf the editor URL requires IDs not present in the public URL (e.g., `pageId=123`), URL transform will not work—use the **Meta tag format** above.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "django_cms",
    "name": "django CMS",
    "category": "Open-source CMS framework (Python/Django)",
    "silktide_support": "custom",
    "edit_link_strategy": "url_transform",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** URL transform (Silktide derives the editor URL by manipulating the public URL).\n**Transform rule:** If the toolbar edit mode is enabled, use an edit-mode flag such as `?edit` (implementation-specific).\n**Notes:** Depends on django CMS toolbar configuration; if uncertain, treat as 'depends' and fall back to meta tag.\n**When to use meta tag instead:** If the CMS editor URL requires internal IDs (e.g., pageId/itemId) that are not present in the public URL.",
    "notes_original": "Many setups allow entering edit mode by appending ?edit to the current page URL (logged-in editors).",
    "editor_context": {
      "strategy": "url_transform",
      "silktide_support": "custom",
      "transform_rule": "If the toolbar edit mode is enabled, use an edit-mode flag such as `?edit` (implementation-specific).",
      "limitations": "Depends on django CMS toolbar configuration; if uncertain, treat as 'depends' and fall back to meta tag."
    },
    "template_md": "# django CMS → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** URL transform (derive editor URL from public URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **django CMS**.\n\n## CMS-specific considerations\nMany setups allow entering edit mode by appending ?edit to the current page URL (logged-in editors).\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use URL transform (recommended)\nIf your CMS editor URL can be derived from the public URL **without internal IDs** (for example: host swap, append `/edit`, or add an edit query parameter), Silktide can deep-link using a URL transform.\nIf the editor URL requires internal IDs (e.g., `pageId=123`), use the **Meta tag format** below instead.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## URL transform (optional)\n\nIf your CMS editor URL can be derived from the public URL using a deterministic string rule (**without internal IDs**), Silktide can deep-link via URL transforms.\n\n**Examples of common transforms**\n- **Host swap:** `https://www.example.com/path` → `https://edit.example.com/path`\n- **Path prefix:** `https://www.example.com/path` → `https://www.example.com/admin/edit/path`\n- **Append segment:** `https://www.example.com/path` → `https://www.example.com/path/edit`\n- **Add query param:** `https://www.example.com/path` → `https://www.example.com/path?mode=edit`\n\n**What to provide**\n- 2–3 example public URLs\n- The matching editor URLs\n- The exact transform rule(s)\n\nIf the editor URL requires IDs not present in the public URL (e.g., `pageId=123`), URL transform will not work—use the **Meta tag format** above.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "modx",
    "name": "MODX",
    "category": "Open-source CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Template head via placeholders/snippets.\n**How to build `editorUrl`:** MODX resources have numeric IDs; emit the current resource ID.\n**Common editor URL pattern(s):** https://{SITE}/manager/?a=resource/update&id={RESOURCE_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Manager edit URLs are resource-ID based; public slugs don’t reliably include IDs.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{SITE}/manager/?a=resource/update&id={RESOURCE_ID}"
      ],
      "id_source_hint": "MODX resources have numeric IDs; emit the current resource ID.",
      "where_to_inject": "Template head via placeholders/snippets."
    },
    "template_md": "# MODX → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **MODX**.\n\n## CMS-specific considerations\nManager edit URLs are resource-ID based; public slugs don’t reliably include IDs.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "silverstripe",
    "name": "Silverstripe CMS",
    "category": "Open-source CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Theme layout head (or controller) where page ID is available.\n**How to build `editorUrl`:** Emit the page record ID used by Silverstripe.\n**Common editor URL pattern(s):** https://{SITE}/admin/pages/edit/show/{PAGE_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Admin edit routes are ID-based (pages/records).",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{SITE}/admin/pages/edit/show/{PAGE_ID}"
      ],
      "id_source_hint": "Emit the page record ID used by Silverstripe.",
      "where_to_inject": "Theme layout head (or controller) where page ID is available."
    },
    "template_md": "# Silverstripe CMS → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Silverstripe CMS**.\n\n## CMS-specific considerations\nAdmin edit routes are ID-based (pages/records).\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "concretecms",
    "name": "Concrete CMS (Concrete5)",
    "category": "Open-source CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "depends",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Depends (URL transform may work in some setups; meta tag is the safe default).\n**Decision rule:** If you can deterministically map the public URL to an editor URL without internal IDs, use URL transform. Otherwise use the meta tag.\n**Context:** Some sites use on-page editing (same URL for editors), others route through dashboard pages with cID. If you can reliably derive edit mode from public URL, use transform; otherwise meta tag with page ID.",
    "notes_original": "Some sites use on-page editing (same URL for editors), others route through dashboard pages with cID. If you can reliably derive edit mode from public URL, use transform; otherwise meta tag with page ID.",
    "editor_context": {
      "strategy": "depends",
      "silktide_support": "custom"
    },
    "template_md": "# Concrete CMS (Concrete5) → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Depends (transform if possible, otherwise meta tag)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Concrete CMS (Concrete5)**.\n\n## CMS-specific considerations\nSome sites use on-page editing (same URL for editors), others route through dashboard pages with cID. If you can reliably derive edit mode from public URL, use transform; otherwise meta tag with page ID.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Prefer URL transform if your setup supports it\nIf your editor URL is derivable from the public URL without IDs, use **URL transform**.\nIf not (common), use the **Meta tag format** below.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## URL transform (optional)\n\nIf your CMS editor URL can be derived from the public URL using a deterministic string rule (**without internal IDs**), Silktide can deep-link via URL transforms.\n\n**Examples of common transforms**\n- **Host swap:** `https://www.example.com/path` → `https://edit.example.com/path`\n- **Path prefix:** `https://www.example.com/path` → `https://www.example.com/admin/edit/path`\n- **Append segment:** `https://www.example.com/path` → `https://www.example.com/path/edit`\n- **Add query param:** `https://www.example.com/path` → `https://www.example.com/path?mode=edit`\n\n**What to provide**\n- 2–3 example public URLs\n- The matching editor URLs\n- The exact transform rule(s)\n\nIf the editor URL requires IDs not present in the public URL (e.g., `pageId=123`), URL transform will not work—use the **Meta tag format** above.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "processwire",
    "name": "ProcessWire",
    "category": "Open-source CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Main layout/head template.\n**How to build `editorUrl`:** Emit the ProcessWire page id ($page->id).\n**Common editor URL pattern(s):** https://{SITE}/admin/page/edit/?id={PAGE_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Admin edit URLs require the page ID; public URL is path-based.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{SITE}/admin/page/edit/?id={PAGE_ID}"
      ],
      "id_source_hint": "Emit the ProcessWire page id ($page->id).",
      "where_to_inject": "Main layout/head template."
    },
    "template_md": "# ProcessWire → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **ProcessWire**.\n\n## CMS-specific considerations\nAdmin edit URLs require the page ID; public URL is path-based.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "octobercms",
    "name": "October CMS",
    "category": "CMS (Laravel/PHP)",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** In the global frontend `<head>` so every page emits a `silktide-cms` meta tag.\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Back-end editing is record-ID based and front-end URLs are developer-defined.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom"
    },
    "template_md": "# October CMS → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **October CMS**.\n\n## CMS-specific considerations\nBack-end editing is record-ID based and front-end URLs are developer-defined.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "statamic",
    "name": "Statamic",
    "category": "CMS (Laravel, flat-file)",
    "silktide_support": "custom",
    "edit_link_strategy": "depends",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Depends (URL transform may work in some setups; meta tag is the safe default).\n**Decision rule:** If you can deterministically map the public URL to an editor URL without internal IDs, use URL transform. Otherwise use the meta tag.\n**Context:** Entry editing URLs in the Control Panel depend on collection/blueprint structure. If your public paths map cleanly to CP entry routes, use transform; otherwise generate editorUrl in meta tag from entry identifiers.",
    "notes_original": "Entry editing URLs in the Control Panel depend on collection/blueprint structure. If your public paths map cleanly to CP entry routes, use transform; otherwise generate editorUrl in meta tag from entry identifiers.",
    "editor_context": {
      "strategy": "depends",
      "silktide_support": "custom"
    },
    "template_md": "# Statamic → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Depends (transform if possible, otherwise meta tag)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Statamic**.\n\n## CMS-specific considerations\nEntry editing URLs in the Control Panel depend on collection/blueprint structure. If your public paths map cleanly to CP entry routes, use transform; otherwise generate editorUrl in meta tag from entry identifiers.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Prefer URL transform if your setup supports it\nIf your editor URL is derivable from the public URL without IDs, use **URL transform**.\nIf not (common), use the **Meta tag format** below.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## URL transform (optional)\n\nIf your CMS editor URL can be derived from the public URL using a deterministic string rule (**without internal IDs**), Silktide can deep-link via URL transforms.\n\n**Examples of common transforms**\n- **Host swap:** `https://www.example.com/path` → `https://edit.example.com/path`\n- **Path prefix:** `https://www.example.com/path` → `https://www.example.com/admin/edit/path`\n- **Append segment:** `https://www.example.com/path` → `https://www.example.com/path/edit`\n- **Add query param:** `https://www.example.com/path` → `https://www.example.com/path?mode=edit`\n\n**What to provide**\n- 2–3 example public URLs\n- The matching editor URLs\n- The exact transform rule(s)\n\nIf the editor URL requires IDs not present in the public URL (e.g., `pageId=123`), URL transform will not work—use the **Meta tag format** above.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "directus",
    "name": "Directus",
    "category": "Headless CMS / data platform",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Frontend head.\n**How to build `editorUrl`:** Emit collection name + item ID used for the page.\n**Common editor URL pattern(s):** https://{ADMIN_HOST}/admin/content/{COLLECTION}/{ITEM_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Editor UI is collection + item-ID based; public URLs are app-specific.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{ADMIN_HOST}/admin/content/{COLLECTION}/{ITEM_ID}"
      ],
      "id_source_hint": "Emit collection name + item ID used for the page.",
      "where_to_inject": "Frontend head."
    },
    "template_md": "# Directus → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Directus**.\n\n## CMS-specific considerations\nEditor UI is collection + item-ID based; public URLs are app-specific.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "datocms",
    "name": "DatoCMS",
    "category": "Headless CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Frontend head.\n**How to build `editorUrl`:** Emit Dato model/item IDs from the CMS payload.\n**Common editor URL pattern(s):** https://{ADMIN_HOST}/admin/item_types/{MODEL_ID}/items/{ITEM_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Editor URLs are model/item-ID based and not derivable from public slugs.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://{ADMIN_HOST}/admin/item_types/{MODEL_ID}/items/{ITEM_ID}"
      ],
      "id_source_hint": "Emit Dato model/item IDs from the CMS payload.",
      "where_to_inject": "Frontend head."
    },
    "template_md": "# DatoCMS → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **DatoCMS**.\n\n## CMS-specific considerations\nEditor URLs are model/item-ID based and not derivable from public slugs.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "kontent_ai",
    "name": "Kontent.ai",
    "category": "Headless CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Frontend head.\n**How to build `editorUrl`:** Emit project ID + item ID (or codename) used for the page.\n**Common editor URL pattern(s):** https://app.kontent.ai/{PROJECT_ID}/content-inventory/{ITEM_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Editor URLs use internal item IDs/codenames; public URL mapping is custom.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://app.kontent.ai/{PROJECT_ID}/content-inventory/{ITEM_ID}"
      ],
      "id_source_hint": "Emit project ID + item ID (or codename) used for the page.",
      "where_to_inject": "Frontend head."
    },
    "template_md": "# Kontent.ai → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Kontent.ai**.\n\n## CMS-specific considerations\nEditor URLs use internal item IDs/codenames; public URL mapping is custom.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "hygraph",
    "name": "Hygraph (GraphCMS)",
    "category": "Headless CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Frontend head.\n**How to build `editorUrl`:** Emit model + entry ID; project slug is customer-specific.\n**Common editor URL pattern(s):** https://app.hygraph.com/{PROJECT_SLUG}/content/{MODEL}/{ENTRY_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Editor is model + entry-ID based; public URLs are app-defined.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://app.hygraph.com/{PROJECT_SLUG}/content/{MODEL}/{ENTRY_ID}"
      ],
      "id_source_hint": "Emit model + entry ID; project slug is customer-specific.",
      "where_to_inject": "Frontend head."
    },
    "template_md": "# Hygraph (GraphCMS) → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Hygraph (GraphCMS)**.\n\n## CMS-specific considerations\nEditor is model + entry-ID based; public URLs are app-defined.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "builderio",
    "name": "Builder.io",
    "category": "Visual headless CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** Frontend head.\n**How to build `editorUrl`:** Emit Builder content ID (and model if applicable).\n**Common editor URL pattern(s):** https://builder.io/content/{MODEL}/{CONTENT_ID} (varies by instance).\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Editing happens inside Builder with content IDs/spaces; public URLs are typically mapped via model/URL fields.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom",
      "editor_url_common_patterns": [
        "https://builder.io/content/{MODEL}/{CONTENT_ID}"
      ],
      "id_source_hint": "Emit Builder content ID (and model if applicable).",
      "where_to_inject": "Frontend head."
    },
    "template_md": "# Builder.io → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Builder.io**.\n\n## CMS-specific considerations\nEditing happens inside Builder with content IDs/spaces; public URLs are typically mapped via model/URL fields.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "brightspot",
    "name": "Brightspot",
    "category": "Enterprise CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** In the global frontend `<head>` so every page emits a `silktide-cms` meta tag.\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Authoring is ID-based and implementation-specific; public URLs don’t reliably encode item IDs.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom"
    },
    "template_md": "# Brightspot → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Brightspot**.\n\n## CMS-specific considerations\nAuthoring is ID-based and implementation-specific; public URLs don’t reliably encode item IDs.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "crownpeak",
    "name": "Crownpeak",
    "category": "Enterprise DX / CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "webhook",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** In the global frontend `<head>` so every page emits a `silktide-cms` meta tag.\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Authoring UI uses internal asset IDs; public URL mapping varies.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom"
    },
    "template_md": "# Crownpeak → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Crownpeak**.\n\n## CMS-specific considerations\nAuthoring UI uses internal asset IDs; public URL mapping varies.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "tridion_sites",
    "name": "Tridion Sites (SDL Tridion)",
    "category": "Enterprise CMS",
    "silktide_support": "custom",
    "edit_link_strategy": "meta_tag",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Meta tag (each page outputs its own editor URL).\n**Why:** The CMS editor URL typically needs internal IDs/UUIDs that cannot be derived from the public URL alone.\n**Where to implement:** In the global frontend `<head>` so every page emits a `silktide-cms` meta tag.\n**Security:** Do not include tokens/session keys in the editorUrl (Base64 is reversible).",
    "notes_original": "Editing is ID-based (items/components/pages) and not derivable from public URLs.",
    "editor_context": {
      "strategy": "meta_tag",
      "silktide_support": "custom"
    },
    "template_md": "# Tridion Sites (SDL Tridion) → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Meta tag (page outputs editor URL)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **Tridion Sites (SDL Tridion)**.\n\n## CMS-specific considerations\nEditing is ID-based (items/components/pages) and not derivable from public URLs.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Use Meta tag (recommended)\nMost CMSs use editor URLs that include **internal IDs** (page IDs, entry IDs, GUIDs). Those IDs are usually not present in the public URL, so URL transforms are not reliable.\nUse the meta tag format below to output the correct editor URL for each page.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  },
  {
    "slug": "hcl_dx",
    "name": "HCL Digital Experience (DX)",
    "category": "Enterprise portal / DXP",
    "silktide_support": "custom",
    "edit_link_strategy": "depends",
    "retest_strategy": "event_handler",
    "notes": "**Deep-link approach:** Depends (URL transform may work in some setups; meta tag is the safe default).\n**Decision rule:** If you can deterministically map the public URL to an editor URL without internal IDs, use URL transform. Otherwise use the meta tag.\n**Context:** Some deployments support in-context editing on the live URL; others require control panel routes with internal IDs. If edit mode can be enabled via URL parameters, use transform; otherwise generate editorUrl via meta tag using internal IDs.",
    "notes_original": "Some deployments support in-context editing on the live URL; others require control panel routes with internal IDs. If edit mode can be enabled via URL parameters, use transform; otherwise generate editorUrl via meta tag using internal IDs.",
    "editor_context": {
      "strategy": "depends",
      "silktide_support": "custom"
    },
    "template_md": "# HCL Digital Experience (DX) → Silktide deep-link integration template\n\n**Silktide support:** Custom (customer-built)  \n**Recommended deep-link approach:** Depends (transform if possible, otherwise meta tag)\n\nUse this template to enable Silktide’s **Edit** button to deep-link users from a page in Silktide to the correct editor screen in **HCL Digital Experience (DX)**.\n\n## CMS-specific considerations\nSome deployments support in-context editing on the live URL; others require control panel routes with internal IDs. If edit mode can be enabled via URL parameters, use transform; otherwise generate editorUrl via meta tag using internal IDs.\n\n## What Silktide needs\nSilktide needs a URL that a user should visit to edit a given public page in your CMS. When a user clicks **Edit** in Silktide, they are sent to that `editorUrl` (and may be prompted to log in).\n\n## Recommended approach\n### Prefer URL transform if your setup supports it\nIf your editor URL is derivable from the public URL without IDs, use **URL transform**.\nIf not (common), use the **Meta tag format** below.\n\n## Meta tag format (Silktide “Edit” button)\n\nSilktide reads a per-page meta tag in the HTML `<head>`:\n\n```html\n<meta name=\"silktide-cms\" content=\"BASE64_ENCODED_JSON\">\n```\n\nThe Base64 value must decode to JSON like:\n\n```json\n{ \"cms\": \"YOUR_CMS_NAME\", \"editorUrl\": \"https://cms.example.com/edit/...\" }\n```\n\n**Rules**\n- `editorUrl` should be an absolute URL that opens the editor for **this specific page**.\n- Do **not** include authentication tokens, usernames, or any secret values (Base64 is reversible).\n- If your editor URL requires internal IDs (page/entry GUIDs), generate this tag dynamically at render time.\n\n### Sample PHP (Base64-encode JSON)\n\nIf your stack can output server-side HTML, you can use this pattern:\n\n```php\n<?php\n$payload = [\n  \"cms\" => \"YOUR_CMS_NAME\",\n  \"editorUrl\" => $editorUrl, // build this per-page\n];\n\n$encoded = base64_encode(json_encode($payload));\necho '<meta name=\"silktide-cms\" content=\"' . htmlspecialchars($encoded, ENT_QUOTES) . '\">';\n?>\n```\n\n(If you don’t use PHP, replicate the same logic in your server-side language.)\n\n## URL transform (optional)\n\nIf your CMS editor URL can be derived from the public URL using a deterministic string rule (**without internal IDs**), Silktide can deep-link via URL transforms.\n\n**Examples of common transforms**\n- **Host swap:** `https://www.example.com/path` → `https://edit.example.com/path`\n- **Path prefix:** `https://www.example.com/path` → `https://www.example.com/admin/edit/path`\n- **Append segment:** `https://www.example.com/path` → `https://www.example.com/path/edit`\n- **Add query param:** `https://www.example.com/path` → `https://www.example.com/path?mode=edit`\n\n**What to provide**\n- 2–3 example public URLs\n- The matching editor URLs\n- The exact transform rule(s)\n\nIf the editor URL requires IDs not present in the public URL (e.g., `pageId=123`), URL transform will not work—use the **Meta tag format** above.\n\n## Validation checklist\n\n1. Open **two different pages** on your site and view page source.\n2. Search for `silktide-cms` and confirm the meta tag exists in the `<head>`.\n3. Base64-decode the `content` value and confirm:\n   - `cms` is correct\n   - `editorUrl` opens the right CMS editor for that page\n4. In Silktide, open a page in the inspector and click **Edit**:\n   - If you’re not logged in to the CMS, you may be prompted to log in first.\n\n## Security notes\n\n- Base64 is **not** encryption; anyone can decode it.\n- Do not embed any values that authenticate a user (tokens, one-time links, usernames).\n- The editor URL should rely on normal CMS authentication/session behavior.\n"
  }
]